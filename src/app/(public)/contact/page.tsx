export default function ContactPage() {
  return (
    <div className="mx-auto max-w-3xl px-4 py-12">
      <h1 className="mb-4 text-2xl font-semibold text-slate-900">Contact</h1>
      <p className="text-sm text-slate-600">
        Interested in a specific property? Open its listing page and use the Inquire or Schedule a Viewing
        buttons — that routes your message directly to the agent handling that unit.
      </p>
    </div>
  );
}
